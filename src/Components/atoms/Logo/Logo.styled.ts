import styled from 'styled-components';
import colors from 'styles/colors';

export const StyledLogo = styled.h1`
    color: ${colors.BasicGreen};
    border: 3px solid ${colors.BasicGreen};
    border-radius: 10px;
    padding: 5px;
    margin: 5px 0;
`;