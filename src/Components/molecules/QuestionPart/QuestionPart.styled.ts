import styled from 'styled-components';
import { FlexColumn } from 'theme/Mixins';

export const StyledQuestionPart = styled.div`
    ${FlexColumn};
    height: 100%;
    background-color: white;
    padding: 10px;

`